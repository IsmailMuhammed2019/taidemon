export default function ProductLayoutDetails ({children} : {children : React.ReactNode}) {
    return (
        <div>
            {children}
            <h2>Featured products goes in here</h2>
        </div>
    )
}